function add(num1, num2) {
    // Conversion des chaînes de caractères en nombres en utilisant Number() si besoin.
    num1 = Number(num1);
    num2 = Number(num2);

    // Retourner la somme des deux nombres
    return num1 + num2;
}

 export default add;
